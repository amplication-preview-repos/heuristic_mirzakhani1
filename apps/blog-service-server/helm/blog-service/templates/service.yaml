apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-service.fullname" . }}
  labels:
    {{- include "blog-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blog-service.selectorLabels" . | nindent 4 }}
